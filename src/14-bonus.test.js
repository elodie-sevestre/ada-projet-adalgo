import { describe, test, expect } from "vitest";
import { initNextButton } from "./14-bonus";

describe("initNextButton", () => {
  test("returns init next button", () => {});
  expect(initNextButton()).toBe();
});
